# Panda Browser Gateway — dépendances Python minimales
# Installer via : pip install -r requirements.txt

# API server
fastapi>=0.115
uvicorn>=0.32
httpx>=0.27

# Browser automation (desktop/CDP mode)
patchright>=1.58

# Config
python-dotenv>=1.0
pydantic>=2.5

# API compatibility
openai>=1.0
